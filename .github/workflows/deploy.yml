name: Build and deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  build-and-deploy:
    name: Build and deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}-${{ hashFiles('**/package.json') }}
          restore-keys: |
            ${{ runner.os }}-node-${{ hashFiles('**/package.json') }}

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          # publish_branch: gh-pages # default is gh-pages; uncomment to override


# - name: Deploy to other (public) repo
#   env:
#     TARGET_REPO: your-user-or-org/target-repo   # change this
#   run: |
#     mkdir deploy
#     cp -a public/. deploy/
#     cd deploy
#     git init
#     git config user.name "github-actions[bot]"
#     git config user.email "github-actions[bot]@users.noreply.github.com"
#     git add -A
#     git commit -m "Deploy to gh-pages from ${GITHUB_REPOSITORY}@${GITHUB_SHA}" || echo "no changes"
#     # force-push local contents to gh-pages on the target repo
#     git push --force https://${{ secrets.DEPLOY_PAT }}@github.com/${{ env.TARGET_REPO }} HEAD:gh-pages
